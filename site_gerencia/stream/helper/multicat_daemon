#!/bin/bash

#echo "[`date`] $@" >> /tmp/log.txt

#nohup /usr/local/bin/multicat $@ >/dev/null 2>&1 &
#sudo -u helber /usr/local/bin/multicat $@ >>/tmp/log.txt 2>&1 &
#sudo -u helber /usr/local/bin/multicat $@ >/dev/null 2>&1 &

nohup /usr/bin/multicat $@ >/dev/null 2>&1 &
PID=$!
#echo "PID:$PID" >> /tmp/log.txt

echo -n "$PID"
#echo ""
exit 0

