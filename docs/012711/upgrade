Dear Helber,
Please download our new firmware at 012711/2011_01_27_libs.rar in our ftp.

login as root, and execute below command to upgrade.

1) umount /opt/qt
2) umount /opt/mrua_dcchd
3) dd if=2011_01_27_nblock8.img of=/dev/nblock8
4) dd if=2011_01_27_nblock9.img of=/dev/nblock9
5) sync

This version fix image distortion and multi-cast lag and flick issue. Please have a
try and please let us know if they are fixed or not. (Or any side-effect?) Thanks in
advance.

In addition, we are continue adding new features, so some function names are
changed. Very sorry about this.

toPlayerInfo( bool on)  --> toSetPlayerInfoVisible( bool on )
toSetScreenMode( bool on ) ---> toSetScreenVisible( bool on )
toGetScreenMode( void ) ---> toGetScreenSetting( void )
toSetVolume( int val ) ---> toSetVolumeUp( void )
                  ---> toSetVolumeDown( void )
toGetVolume( void )   ---> toGetVolumeLevel( void )  // +4~-4


Regards,
Frank
